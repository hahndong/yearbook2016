<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>

    <%= f.input :facebook, label: false, placeholder: "FACEBOOK (www.facebook.com/jon.snow)"%>
    <%= f.input :mobile_number, label: false, placeholder: "MOBILE NUMBER"%>

  
    <%= f.button :submit, value: "SUBMIT", id: "profile_submit" %>
  
<% end %>
